import { ChatBot } from '../../../common/ChatBot';

export default function ChatWindow({ currentBot }: { currentBot: ChatBot }) {
	return <div className='border-amber-600 min-h-screen h-screen w-max flex p-8'>
		hi mom
	</div>
}
